---
all:
  vars:
    ansible_user: "root"
    ansible_port: 22
  children:
    qemu:
      hosts:
        testing:
        vm-110:
        vm-111:
        vm-112:
        vm-113:
    k3s:
      hosts:
        k3s-server:

