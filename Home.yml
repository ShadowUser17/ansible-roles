---
all:
  vars:
    ansible_user: "root"
    ansible_port: 22
  children:
    qemu:
      hosts:
        testing:
    k3s:
      hosts:
        k3s-server:
        k3s-agent-one:
        k3s-agent-two:

