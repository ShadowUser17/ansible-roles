---
- hosts: "all"
  name: "Prepare VM"
  become: true
  # debugger: "on_failed"
  roles:
    - "packages"
    - "configs"
    - "users"
    - "repos"
    - "nodejs"
    - "golang"
    - "nerdctl"
    - "aws_tools"
