---
all:
  vars:
    ansible_user: "vagrant"
    ansible_port: 22
  children:
    main:
      hosts:
        testing:
    test:
      hosts:
        k3s-single:
    k3s:
      hosts:
        k3s-server:
        k3s-agent:
