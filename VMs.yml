---
all:
  vars:
    ansible_user: "vagrant"
    ansible_port: 22
  children:
    testing:
      hosts:
        testing:
    k3s_servers:
      hosts:
        k3s-server:
    k3s_agents:
      hosts:
        k3s-agent:
