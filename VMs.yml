---
all:
  vars:
    ansible_user: "root"
    ansible_port: 22
  children:
    debian:
      hosts:
        vm-testing:
        vm-storage:
    fedora:
      hosts:
        vm-k3s-server:
        vm-k3s-agent:
